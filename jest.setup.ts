import '@testing-library/jest-dom';

// (om du använder next/image i komponenter, mocka till <img/>):
// jest.mock('next/image', () => ({
//   __esModule: true,
//   default: (props: any) => <img {...props} />,
// }));
